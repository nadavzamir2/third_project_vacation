import { useEffect, useState } from "react";

export const MetricsPage = () => {

    useEffect(() => {
    }, []);

    return (
        <div>
            <h1>Metrics Page</h1>
        </div>
    );
}